function somar (a: number, b: number) {
    return a + b;
}

const num1 = 10;
const num2 = 5;   // se for string o JS faz funcionar (concatena)

const resultado = somar (num1,num2);
console.log(resultado);